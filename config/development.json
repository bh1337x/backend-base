{
  "$schema": "./schema.json",
  "cors": {
    "origins": ["http://localhost:3000"],
    "methods": ["GET", "POST", "PUT", "DELETE", "OPTIONS"],
    "headers": ["Authorization", "X-HTTP-Method-Override"],
    "credentials": true
  },
  "body": {
    "limit": "512KB"
  }
}
