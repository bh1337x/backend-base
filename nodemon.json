{
  "$schema": "https://json.schemastore.org/nodemon.json",
  "watch": ["src", ".env", "config/*.json"],
  "colours": true,
  "ignore": ["src/**/*.spec.ts"],
  "ext": "ts,json",
  "execMap": {
    "ts": "ts-node"
  }
}
